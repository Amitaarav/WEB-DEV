const a="a";
const b="b";
if(a<b){
    console.log(`${a} is less than $ {b}`);//use{``} always
}else if(a>b)
{
    console.log(`${a} is greater than ${b}`);
}
else 
{
    console.log(`${a} and ${b} are equal`);
}